<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/dark_theme_actionbar</item>
        <item name="colorPrimaryDark">@color/dark_theme_actionbar</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="habit_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:background">@color/dark_theme_background</item>
        <item name="android:textColor">@color/dark_theme_title</item>
    </style>

</resources>
